export const metadata = {
    title: "Profile",
    description: "Manage your sketches, teams, and billing settings.",
    robots: { index: false, follow: false },
};

export default function ProfileLayout({ children }) {
    return children;
}
