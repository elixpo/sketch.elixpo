export const runtime = 'edge'

export const metadata = {
  title: 'Workspace Templates · LixSketch',
  description: 'Discover public LixSketch workspaces and create your own encrypted copy.',
}

export default function TemplatesLayout({ children }) {
  return children
}
