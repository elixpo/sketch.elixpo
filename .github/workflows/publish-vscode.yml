name: Publish VS Code Extension to Marketplace

on:
  push:
    branches:
      - main
    paths:
      - 'packages/vscode/**'
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump kind (patch / minor / major). Default: patch"
        required: false
        default: 'patch'

permissions:
  contents: write

jobs:
  publish:
    name: Publish to VS Code Marketplace
    runs-on: ubuntu-latest
    # Skip when the triggering commit is the bot's own bump (loop guard).
    if: |
      github.event_name == 'workflow_dispatch' ||
      !contains(github.event.head_commit.message, 'chore(release): bump elixpo.lixsketch')
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.CI_AGENT_TOKEN || secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Configure git as elixpoo bot
        run: |
          git config --global user.name "elixpoo"
          git config --global user.email "269200728+elixpoo@users.noreply.github.com"

      # ── Decide the next version ─────────────────────────────────────
      # Compare local package.json version against what the VS Code
      # Marketplace currently has. If equal, bump (patch by default).
      - name: Compute next version
        id: version
        working-directory: packages/vscode
        run: |
          LOCAL=$(node -p "require('./package.json').version")
          REMOTE=$(npx --yes @vscode/vsce show elixpo.lixsketch --json 2>/dev/null \
            | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{try{const j=JSON.parse(s);console.log(j.versions?.[0]?.version||'0.0.0')}catch{console.log('0.0.0')}})" \
            || echo "0.0.0")
          BUMP="${{ github.event.inputs.bump || 'patch' }}"

          echo "Local  version: $LOCAL"
          echo "Remote version: $REMOTE"
          echo "Bump kind: $BUMP"

          if [ "$LOCAL" = "$REMOTE" ]; then
            echo "Versions match — auto-bumping ($BUMP)."
            # See publish-npm.yml for why we don't capture `npm version`
            # stdout: workspaces emit a 2-line string that breaks
            # $GITHUB_OUTPUT parsing.
            npm version "$BUMP" --no-git-tag-version > /dev/null
            NEW=$(node -p "require('./package.json').version")
            echo "Bumped to $NEW"
            echo "version=$NEW" >> "$GITHUB_OUTPUT"
            echo "auto_bumped=true" >> "$GITHUB_OUTPUT"
          else
            echo "Local already ahead of remote — using $LOCAL."
            echo "version=$LOCAL" >> "$GITHUB_OUTPUT"
            echo "auto_bumped=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build extension
        working-directory: packages/vscode
        run: npm run build

      - name: Publish to VS Code Marketplace
        working-directory: packages/vscode
        run: npx @vscode/vsce publish --no-dependencies
        env:
          VSCE_PAT: ${{ secrets.VSCODE_LIXSKETCH_EXT_PUBLISH_TOKEN }}

      # ── Commit the auto-bumped version back to main ─────────────────
      - name: Commit and push version bump
        if: steps.version.outputs.auto_bumped == 'true'
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          git add packages/vscode/package.json packages/vscode/package-lock.json 2>/dev/null || \
            git add packages/vscode/package.json
          git commit -m "chore(release): bump elixpo.lixsketch to v${VERSION}

          Auto-bumped by the publish workflow after a change under
          packages/vscode/** was merged without a manual version bump.

          [skip ci]"
          git push origin HEAD:main

      - name: Tag the release
        if: steps.version.outputs.auto_bumped == 'true'
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          git tag -a "vscode-lixsketch-v${VERSION}" -m "elixpo.lixsketch v${VERSION}"
          git push origin "vscode-lixsketch-v${VERSION}"

      - name: Summary
        run: |
          echo "### Published elixpo.lixsketch@${{ steps.version.outputs.version }} 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Marketplace: https://marketplace.visualstudio.com/items?itemName=elixpo.lixsketch" >> $GITHUB_STEP_SUMMARY
          echo "- Auto-bumped: \`${{ steps.version.outputs.auto_bumped }}\`" >> $GITHUB_STEP_SUMMARY
